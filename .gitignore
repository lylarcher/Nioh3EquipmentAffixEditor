# Byte-compiled / cache
__pycache__/
*.py[cod]
*.pyo

# Editor state
_nioh3_equipment_affix_backup/
state/

# Test scratch
*.tmp
tmp/

# Build artifacts (generated by build.ps1 / tools/make_build_info.py).
# They are per-build identity stamps, not sources: a fresh checkout reports
# live git information until the next build regenerates them.
dist/
BUILD-INFO.json
BUILD-INFO.txt
nioh3_equipment_affix_editor/_buildinfo.py

# PyInstaller scratch space, the embedded payload zip and smoke-test trees.
build/

# Build-time virtual environment that provides PyInstaller (see build.ps1).
.build-venv/

# Local overrides
.env

files/
# 更新日志 / Changelog

本文件记录 Nioh3AccessoryEditor 的所有重要变更。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

> **仅供测试学习用，不要用于联机影响游戏平衡。**

本仓库有一个 `gitea` 远端，因此条目用提交号前缀标注（`git log --oneline` 显示的
7 位），便于和 `BUILD-INFO`、发行包文件名互相对照。注意 `build.ps1` 打印的
`commit` 是**提交号后 8 位**（本项目约定，见 [README 的 Version information](README.md#version-information)），
两者指同一个提交。

每个版本对应的构建身份（commit 后 8 位 / 来源 / 构建时间 / 语言）由
`build.ps1` 在构建时生成并写入 `BUILD-INFO.txt`、`BUILD-INFO.json` 与
`nioh3_accessory_editor/_buildinfo.py`；这些文件属于构建产物、不入库。

---

## [未发布]

### 修复

* **「同名固定」词条改为以存档自己的 metadata 为准，不再只信 xlsx**。规则（用户实测）：
  只要槽位 metadata 带「固定」属性（byte 9 bit 6，`0x4000`）就是同名固定词条，即使词条
  代码不在词条表里；这同样适用于魂核与饰品。原实现只对表内 id 认这个位，表外一律当普通
  词条，于是像 `0xd0c5 造成雷属性伤害时吸取体力 B+` 这类**表外的同名固定**还能被改写。
  现在 `slot_is_fixed()` 的表外分支也认这个位，**唯一例外是恩宠/套装槽**（它们同样带这个
  位，码是 `0x4C00`，但改恩宠走【恩宠】栏，`grace_db` 传进来时按恩宠表排除）。
  影响面（参考存档实测）：饰品池新增 **830** 个不可编辑槽、魂核池新增 **27** 个——都是
  原本能改的同名固定词条。`assert_single_affix_per_category()` 里「★ + 同名固定」的例外
  也换成同一个判定口径。

### 新增 / 修复

* **合法规则：同一个物品里每个「种类」只能有一个词条**。引擎新增
  `assert_single_affix_per_category()`，`plan_edits()` / `plan_soul_edits()` 在写入前
  按 metadata 的种类码（bits 8..12）分组检查，**新出现**两个同种类词条即拒绝并列出是
  哪几个槽。两条例外按游戏实测实现：
  * `其他` 种类可以有多个（恩宠/套装槽的码就是 `其他`，所以不受这条限制）；
  * 同种类的「一个 ★（星号）」与「一个同名固定」可以并存，**但不能有第三个**——
    ★ + 普通同种类词条仍然被拒绝。
  只看「新出现」的重复，旧记录本来就异常时不会被无关改动卡住；饰品与魂核两条写入
  路径都生效。
* **魂核页签：固定词条的数值框现在真的置灰、不能填**。原因是选中魂核后
  `_set_soul_controls(True)` 会把所有魂核数值框一并启用，把逐槽的禁用状态冲掉。现在
  `_set_soul_controls(True)` 改为调用 `_apply_soul_slot_states()`，逐槽按「是不是固定
  词条槽」决定启用与否，固定槽保持禁用并显示存档原值。

### 修复

* **改写词条时，「词条种类」也一起改写（游戏里那个小图标不再停留在旧词条上）**。
  原先只写 id 与数值，metadata 原样不动，所以游戏里词条前的图标（种类）还是旧词条的
  ——用户报告「修改了但词条本身的种类未被修改」。现在：
  * 新表 `data/affix_categories.json`：类别 → metadata 的 bits 8..12（mask `0x1F00`）。
    实测证据：参考存档 893 个饰品槽 + 544 个魂核槽，**15 个类别与码值一一对应、0
    冲突**（饰品与魂核共用同一套码），`★` 位一致 188/189、固定位一致 457/457。
  * 写入时只改这 5 位（`editor.affix_metadata()`），其余位（固定 `0x4000`、★
    `0x040000`、每份副本各自的标志与 byte11）原样保留；只改数值不动词条的编辑完全不
    触碰 metadata。
  * 类别在表里查不到时**拒绝写入**（fail closed），而不是写出一个与新词条不符的图标。
  * 魂核页签同样处理（同一套码）。

### 变更

* **exe 不再出现黑色命令行窗口（cmd 黑框）**：打包从 console 子系统改为
  **windowed** 子系统，程序不再创建控制台窗口，因此双击启动（含随附文件解压）时只有
  界面，也不会因为那个黑框被关掉而退出。命令行子命令（`list` / `check` / `edit` …）
  的输出不受影响：启动时会先复用调用方重定向给它的标准输出，其次才
  `AttachConsole` 借启动它的那个终端，所以“在 cmd 里跑子命令仍能看到输出”，而在
  资源管理器里双击运行时没有终端可借、日志直接丢弃（GUI 用弹窗报错）。GUI 路径**从不**
  挂接任何控制台，任何 cmd 窗口被关掉都不会影响它继续运行。

### 新增 / 修复

* **一件物品只能有一个恩宠/套装词条（游戏规则写入程序）**：引擎新增
  `assert_single_grace()`，`plan_edits()` / `apply_edits()` 接受 `grace_db` 后会在
  写入前检查计划的记录——**新出现**两个恩宠/套装词条即拒绝并说明是哪两个槽（本来就是
  两个的旧记录不会被无关改动卡住）。GUI 与 CLI 都会传该参数，规则对两条路径同时生效；
  槽内编辑本来就无法写入恩宠 id（目录闸门会拒绝），这条检查是第二道保险。
* **表外但不是恩宠/套装的词条恢复为可直接在槽内修改**：`0x30fe 恩宠/套装词条（表外）`
  这种槽既不在词条表里、也不在恩宠表里，之前被 `grace_slots()` 的表外启发式一起禁用，
  现在只按**恩宠表**判定：恩宠表认得 id 的槽才显示为禁用并标注「（用下方【恩宠】栏
  替换）」，其余表外 id 按普通词条处理，槽内可直接替换。

### 新增 / 修复

* **词条槽支持多关键词（空格分隔＝同时包含）**：`星 恢复` 只列出名称或类别里**同时**
  含「星」和「恢复」的词条（例如命中 `(星)体力 +38`，它属于「恢复体力」类别）。
  `AffixDb.search()` 改为按空白切分后逐词 AND，单个关键词行为不变；GUI 与
  `--search` / `--edit 槽:关键词` 一致。槽下方的状态行仍会报告该槽匹配到多少条。
* **恩宠/套装槽不再阻断「应用修改」**：这类槽的名称来自 词条总目录，本来就不是词条
  目录里的标签，之前同样会报「槽N 的文本不是词条表里的词条」。现在它与固定槽一样：
  显示为**禁用**状态并在后面标注「（用下方【恩宠】栏替换）」，数值框一并禁用，收集
  改动时整槽跳过——要换恩宠请用下面的【恩宠】栏。
* **种类 与 恩宠/套装 筛选改为级联**：选了 种类 后，恩宠/套装 只列出该种类记录里
  **实际出现过**的恩宠/套装，反之亦然；当前选择若在新列表里没有，会以
  「xxx（当前无记录）」形式保留，结果为空时状态行会说明是筛选造成的，而不是悄悄
  重置用户的选择。

### 修复

* **固定词条槽不再让「应用修改」失败，其数值框也改为不可填写**。槽里若是固定词条，
  下拉框显示的是带「（固定，不可修改）」后缀的标签，而该后缀不属于任何目录；改成
  每槽独立搜索后取值统一走解析函数，于是只要选中一条含固定词条的饰品，应用修改就会
  报「槽N 的文本不是词条表里的词条」而整次中断。现在：
  * 收集改动时先跳过固定槽，GUI 永远不为固定词条生成改动；
  * 固定槽的数值框改为**禁用**并显示存档原值，提示写「固定词条，词条与数值都不能改」；
  * 切换记录时复位数值框状态，避免上一条记录的固定槽把新记录留在禁用状态；
  * 魂核页签同样处理（新增 soul_value_entries 列表）。

### 修复 / 变更

* **词条槽改成「每槽独立搜索」**：原来界面顶部那一个「关键词 / 搜索词条 / 显示全部」
  会把 **7 个词条槽的下拉列表一起换掉**，而 `readonly` 的 `ttk.Combobox` 在当前值不在
  `values` 里时会**把自己的显示清空** —— 于是点一次搜索，已经填好的槽（以及「固定，
  不可修改」的槽，它的后缀本来就不在任何目录里）看起来就空了，重新点选该饰品才恢复。
  现在每个槽都是一个**可编辑下拉框**：
  * 在某个槽里输入关键词只过滤**该槽自己的**下拉列表，其它槽的文本与列表都不动；
  * 直接展开下拉框也能选（下拉列表就是该槽当前的候选）；
  * 输入可以是关键词、`0x646b`、`646b` 或十进制 id；按回车时若只剩唯一匹配就直接选中，
    若不唯一则提示从该槽下拉列表里选一条；
  * 清空输入即恢复该槽的完整目录（其它槽同样不受影响）；
  * 选中后槽下方的灰字改为显示「词条 0x….，合法数值 …」。
  界面顶部不再有关键词输入框（`search_var`/`soul_search_var` 已移除），保留下方的
  状态行用于报告该槽匹配到多少条。魂核页签同样处理。
* **词条槽不再被裁掉**：每个槽改成两行布局（上行「槽N + 下拉框」，下行「数值 + 提示」）。
  原来 7 个控件挤在一行、用 `pack(side=LEFT)` 排布，数值框会被挤出可见区域；现在每行
  只需 384px，而编辑器列宽 540px。数值框右侧**本来就没有任何按钮**，被挤掉的只是它
  左边的灰色提示文字。

### 变更

* **GUI 的「读取饰品」按钮改名为「读取数据」**：现在一次读取会同时填好 饰品 与
  魂核 两个页签，所以按钮名不再只提饰品；各处提示语也统一成「请先读取数据…」。
  内部方法名 `load_accessories` 保持不变，避免破坏既有调用与测试。

### 新增

* **五项新增编辑能力（筛选 / 关键词 / 数值区间 / 无中生有）：引擎、CLI 与 GUI 全部打通**。
  1. **固定词条只减不增**：同种类饰品/魂核不论原本有没有固定词条，都禁止再加一个——
     0→1、1→2、2→3 三种情况全部拒绝（`FixedSlotAdditionTests` 逐条覆盖），
     新建物品时同样禁止新增或改写固定词条；固定槽的拒绝**先于**数值校验，
     所以报错是「同名固定词条不能修改」而不是数值越界。
  2. **可改词条数值并限制上下限**：词条库现在带 `value_min`/`value_max`。
     数值区间的来源查清了：**饰品词条表本身没有区间列**，区间在
     `全词条数值(3稀有度…)` 的 **取值集合** 列，键是词条 id 的**低 16 位**
     （`代码` 列是小端，`BC 53` → `0x53BC`）；**魂核**区间在 `绘卷-魂核词条` 的
     `数值区间`/`数值集合`。重新生成后 **饰品 276/276、魂核 287/287** 全部带区间。
     实测参考存档：**807 个库内槽位的数值 807/807 都落在各自区间内，0 例外**——
     这就是把区间当写入门禁的依据。越界或非数字在写入前直接拒绝并给出区间；
     原始表里没有区间的词条只允许写目录值（绝不猜）。
     注意：多数饰品词条是**单值**（如「火抗性 +10」区间就是 {10}，数值即词条身份），
     真正可调的是表里给了跨度的行——本表饰品只有一个：`0xfb24 获得魂核时恢复体力` 158..230。
     GUI 每个槽新增数值输入框（选中记录时预填存档值并显示「可改区间」），
     CLI 用 `--edit 槽:名称或id:数值`。
  3. **读取后按种类 / 恩宠·套装筛选**：GUI 饰品页有种类下拉（88 项）与
     恩宠/套装下拉（78 项）+「清除筛选」，状态行显示「筛选后 31 / 213 件」；
     魂核页有种类下拉（84 项），实测「筛选后 4 / 132 个」。
     CLI：`list --kind 八尺琼 --grace 稻荷神`（实测 213 件中 38 件）。
     筛选只影响列表显示，不影响任何写入。
  4. **关键词匹配词条**：GUI 关键词框点「搜索词条」后，把**全部**匹配灌进 7 个词条框供选择；
     零匹配明确提示「「X」没有匹配到任何词条」，「显示全部」恢复全库。
     CLI `edit --search <关键词>` 只搜不改（测试断言存档字节不变）；
     `edit --edit 槽:关键词[:数值]` 在 **0 条**或**多条**时都报错并列出候选
     （实测「伤害」匹配 64 条 → 拒绝并要求写得更具体或用 id），
     唯一匹配才解析写入——歧义关键词永远不会被悄悄取第一条。
  5. **无中生有**：不改现有物品，直接生成一件设置好词条的饰品/魂核放进空槽。
     空槽判据是 `type == 0`（实测参考存档 2000 槽中 **465 个空位**，散布分布、
     其中 209 个两侧都被占用、槽内无残留词条）。新记录以**同种类的真实样本**为模板
     逐字节复制，只重写种类（两处镜像）、等级（两处镜像）与所选词条；
     这是刻意保守：`+0x18` 在记录间有 6 种取值，而 `+0x1c`/`+0x20`/`+0x28`
     **逐件唯一**（213/213），其含义未解码，凭空编造就是猜——所以存档里没有该种类样本时
     直接拒绝并说明「存档里没有 … 的样本」，一个字都不写。固定词条按模板带入且不可改。
     **背包满时**拒绝并提示「背包已满：记录表 2000 个槽位全部被占用，
     请先在游戏里清理背包后再添加」。CLI `create --kind … [--soul]`，
     GUI 两个页签各有「无中生有」区块（实测 dry-run：`八尺琼勾玉[武士]` 与
     `魑魅的魂核` 分别以模板 #135 / #12 写入第一个空位 #1，空位 465）。
* **八咫镜两个 id 按实测拆分（补上 `0x5c5f`）**。`物品总目录` 里
  `八咫镜[武士]` 与 `八咫镜[忍者]` **两行的代码都是 `21 15`**（→ `0x1521`），
  原表根本无法表达第二个 id，生成器只是「先到先得」把 `0x1521` 叫成武士版。
  参考存档给出了判定证据：`#1724`（item `0x5c5f`）的固定词条是
  `0xd45b 格挡可增加灵力 +10`，`#1726`（item `0x1521`）的是
  `0xb3fd 识破可增加灵力 +10`。因此 `0x5c5f → 八咫镜[武士]`（武士饰品）、
  `0x1521 → 八咫镜[忍者]`（忍者饰品），两条都带 `source` 证据写进 JSON；
  物品种类表 88 → **89 条**，实测存档 **213/213** 件饰品都有种类名。
  这还修掉一个潜在误操作：旧命名下 `0x1521 → 0x5c5f` 会被当成同分类互换放行，
  现在按实测分类，跨分类（忍者↔武士）被正确拒绝。注意八咫镜有**两个固定槽**
  （0、1），只有固定槽布局相同的同分类样本才能与它互换（参考存档里没有这样
  的样本），但八咫镜自己可以当「无中生有」的模板复制。
* **`+0x0A` 已确认为物品的 `+値`，成为一等可改字段**。此前它是唯一未核实的头部字段；
  现在有了游戏内结论：三件同种类饰品在物品卡上显示 **+13 / +18 / +19**，
  正好等于本工具读到的记录 **#28 / #3 / #521** 的 `+0x0A`——映射是 **1:1**，
  「是 +値 两倍」的旧猜测已被这条证据否掉并在注释里记明。
  * 引擎：`records.read_record_plus`（旧名 `read_record_plus_candidate` 保留为
    已废弃别名）、`patch_record_plus`、`MAX_RECORD_PLUS = 30`；
    `editor.AccessoryView.plus_value`（原 `plus_candidate`）、
    `PlusPlan` / `plan_plus_edit` / `apply_plus_edits`。
  * CLI：`edit --record N --plus V`。与既有五种模式并列，模式数从「五选一」改为
    **六选一**；越界（`0..30` 之外）、非整数、改成原值都在写入前拒绝。
  * GUI：饰品页签新增 **+值** 列与 `+值（0..30，字段 +0x0A）` 一行（输入框 + 应用按钮 +
    状态行，行为与「等级」一行一致）；魂核页签仍只读显示。
  * 范围仍是 **0..30**，依据是参考存档实测取值（213 件饰品 0..30、25 种取值，
    魂核 0..18）；**游戏自身的上限没有可核对的来源**，所以不写进帮助文本当事实，
    而是在 README 与本条里写明这是实测范围。
  * 只写 `+0x0A` 这两个字节与校验和：词条、等级、标识、镜像一概不动，
    并由测试逐字节断言（`PlusValueEditTests`）。
* **写入前新增「停在标题界面」的用户确认（GUI 勾选框 / CLI `--at-title-screen`）**。
  游戏运行中不再一律拒绝：只要操作者明确确认游戏**停在标题界面**（尚未载入存档），
  就可以写入；没有这个确认就是**拒绝**，不是警告。
  * GUI 窗口底部新增勾选框「我确认：游戏正在运行，但停留在标题界面（尚未载入存档）」，
    只探测到进程时才起作用；**每次写入成功后自动取消勾选**，
    所以下一次写入必须重新确认（`TitleScreenGateTests` 覆盖两个写入路径）。
  * 游戏状态行与拒绝对话框都直接写明两条出路：完全退出游戏，或退回标题界面并勾选确认。
  * CLI：`--at-title-screen`；`--force-while-running` 保留为同一确认的旧名（效果相同），
    `edit` / `create` / `restore` 三个子命令都提供。
* **`tools/set_plus.py`：仍作为「单字段写入」的对照脚本保留**。
  `--record N --value V`（0..30），默认只演练、`--write` 才写，写入前自动备份，
  并强制校验「整个存档只有 `+0x0A` 的字节发生变化」，否则中止；同样接受
  `--at-title-screen`。日常改 +値 请优先用 `edit --plus`，这个脚本只是同一个编辑的
  无子命令版本。副本实测：`#28 龙笛[武士]` `+0x0A` 13 → 0，只有 1 个字节改变，
  写入后复查为 0。
* **魂核（魂之核）页签：独立的词条库与种类表，同样只做合法修改**。
  魂核**没有恩宠/套装词条**，所以它单独的判定与词条库来自原表 `绘卷-魂核词条` 里
  `种类=魂核` 的 **287 条**（与饰品词条库只有 1 个 id 重叠：`0xfb24`），
  种类来自 `物品总目录` 里 `大类=魂核` 的 **83 条**。
  识别规则是两条独立证据同时成立：**头部物品 id 是魂核种类表里的一行**，且**至少
  一个词条槽命中魂核词条库**——只用其中一条都会误判（那 1 个重叠 id 会让
  `0x4987` 八尺琼勾玉[武士] 之类的饰品命中 1 次）。
  实测参考存档：候选 137 条，**识别出 132 条**；剩下 5 条全部拒绝（2 条 `0xda62`
  不在魂核种类表内，3 条是带 `0xfb24` 的饰品），界面里明确写"未识别，本工具不会改它"。
  其中 132 条与原始表的 `固定词条代码` **逐条一致，0 处不符**。
* **魂核页签的三个修改**：改魂核词条（只允许魂核词条库内的 id）、改等级（
  `1..180`，只写 `+0x06/+0x08`）、改种类（**魂核 ↔ 魂核**，固定词条按新种类的
  真实样本自动同步）。同名固定词条**不可修改**——实测 132 条魂核各带 2 个固定词条，
  页签里这两行的下拉框直接置灰并标注"固定词条，禁止修改"。
* **GUI 改为页签（Notebook）**：`饰品` 页签保持原有布局，新增 `魂核` 页签；
  魂核页签按同样的三行（词条槽 / 等级 / 种类）组织，并显示"魂核没有恩宠/套装词条"。
* **CLI `souls` 命令**列出存档里的魂核记录与词条（只读）；`edit --soul` 把
  `--edit/--level/--kind` 切到魂核词条库与魂核种类表；`--soul --grace` 直接报错
  （魂核没有恩宠/套装词条）。
* **新增 绿色星号词条（★）工作表：饰品 32 条、魂核 51 条，并支持「数值集合」**。
  这张表共 257 行，**A 列写明每个 ★ 词条能出现在哪些装备上**（`[饰品]`、`[魂核]`、
  `[近战/手臂]`…）。这 257 个 id 与 饰品词条 / 绘卷-魂核词条 **0 重合**（实测），
  所以在此之前工具一个 ★ 词条都提供不了。
  * 只有 A 列含 饰品/魂核 的行才会进入对应目录：饰品 **32** 条、魂核 **51** 条
    （3 个 id 两者都合法）；只列 近战/远程/防具 部位的行绝不进入饰品目录，有专门
    测试守着。两表由此变为 **308**（276+32）/ **338**（287+51）条。
  * **数值区间** 用该表 E/F 列，写入前照旧按区间拒绝越界值。
  * **数值集合**（G 列起）在**不等于整段区间**时按集合保留：33 行是 0.1% 单位的
    2.5 步进（如 `0x248c` 只允许 470、472、475…520），因此 `AffixEntry` 新增
    `values` 字段，`allows_value()` 优先按集合校验——471 落在 470..520 之内也会被
    拒绝，因为游戏不会掉这个值。JSON 里只在非整段时写 `values`。
  * 257 行中有 **3 行**编码的数值被自己的区间排除（`0xdfae`：188 对 127..140；
    `0x248c`：250 对 470..520）；生成器把目录默认值收进区间，避免输出一个编辑器
    随后会拒绝写入的默认值。
  * 星号本身是元数据第 10 字节的第 2 位（`is_star`），★ 词条是普通可改槽位。
  * 测试：`GreenSheetTests` 11 项（两表条数、共有 id、区间/集合、越界拒绝、
    近战专属行不入饰品目录、与原两表 0 重合、0x248c 步进集合的行为）。
* **修复：魂核页签的编辑区根本没显示出来**。魂核页签只建好了左侧记录表，
  右侧的词条槽 / 等级 / 种类 / 无中生有整列**从未加入它自己的左右分栏**
  （`mid.add(right_scroll, weight=3)` 漏了），所以那个页签看起来「只能看不能改」。
  现已补上，并加了守卫测试（两个页签各自必须有两个分栏，且编辑器列里必须有控件）。
* **界面不再截断：编辑器列改为可滚动，页脚版本信息收成一行**。之前编辑器列内容
  （词条槽 + 等级 + +值 + 种类 + 新建）比窗口高，`pack()` 会把放不下的部分**静默裁掉**，
  于是新加的「+值」一行看不到。现在饰品与魂核的编辑器列各自放在 canvas + 滚动条里
  （鼠标滚轮可用），默认窗口改为 1280×900（最小 1040×660）；页脚的构建信息收成
  一行 `v0.1.0 · commit … · 构建 … · 语言 …`，完整信息仍由「版本信息」按钮弹出。
  读取完成后状态行会写「魂核 N 个已读好，见上方的『魂核（魂之核）』页签」，
  魂核页签标题也改成 `魂核（魂之核）`。
* **同名固定词条禁止修改（fail closed）**：每种饰品都有它自己的固定词条（有的种类没有）。
  判定用两份证据：词条库里该 id 带 `(同名固定)`，以及槽位 metadata 第 9 字节的 `0x40` 位。
  在参考存档上实测：**库里能对上的 795 个槽位，两者完全一致，0 例外**。
  注意这个 `0x40` 位**不能单独用来判固定**——恩宠槽（字节 9 = `0x4C`）与套装槽也带这一位，
  所以只有「在库内」时才会采信它（这条是测试发现的真问题，已修：
  `test_the_marker_bit_alone_is_not_consulted_outside_the_catalog`）。
  固定槽在 `list`/GUI 里显示成 `（固定词条，不可修改）`，GUI 下拉框置灰；
  `plan_edits` 既拒绝**改**固定槽，也拒绝把普通槽**写成**固定词条 id
  （否则会描述出一件不可能掉落的饰品）。唯一会重写固定槽的是下面的「改种类」。
* **可以改等级了（上限 180）**：`edit --record N --level 180`，或 GUI 右侧新增的
  「等级（上限 180）」栏。只写 `+0x06` 与其镜像 `+0x08`，范围 1..180——
  180 的依据是参考项目把有效等级读成 `min(record +0x06, 180)`，而参考存档里最大值正好是 180。
  拒绝 0/负数/>180、拒绝「改成原值」、拒绝两个等级字段不一致的记录
  （213 件饰品上两者全部相等，不一致说明不是工具验证过的形态）。
  在真实存档**副本**上跑完整写入流程验收：9.4 MB 存档只变 **5 个字节**
  （等级 2 字节 + 校验和 3 字节），写回后读回 `Lv180`（镜像也是 180）。
  **谨慎修改**提示写在 CLI 输出与 GUI 确认框里，理由见下一条。
* **等级与词条数值的关系（已实测，所以只改等级）**：存档里同种饰品的**词条数值不随等级变化**——
  例如 除雷护身符[武士] 在 156..170 级之间的每一件都是 `雷属性伤害降低 +15`，
  八尺琼勾玉[武士] 31 件（153..170 级）都是固定词条 `数值=1`。
  所以游戏里看到的数值提升只能是游戏读档后**自己算出来的**，存档文件无法证明；
  这正是「只改等级 + 提示谨慎修改」的原因。`+值` 当时不写入（头部唯一一个逐件变化的
  计数 `+0x0A` 那时只是候选字段）；该字段后来**已在游戏内确认**，现在是一条独立的
  可改路径，见本文件「未发布」一节。
* **可以改种类了（仅同分类互换 + 固定词条自动同步）**：`edit --record N --kind 八尺琼勾玉[武士]`
  （也可用 id `--kind 0x4987`），或 GUI 右侧的「种类（同分类互换）」栏。三条 fail-closed 规则：
  1. **只能同分类**：当前 id 与目标 id 都必须在 `物品总目录` 里有中类，且中类相同
     （`武士饰品` ↔ `武士饰品`、`忍者饰品` ↔ `忍者饰品`）；跨分类、或表里没有的 id
     （包括 `0x5c5f`）一律拒绝并说明理由。
  2. **固定词条是从真实样本复制的，不是编造的**：目标种类的固定词条（槽位/id/数值/
     metadata 第 9 字节）取自**参考存档里已有的同种类实例**。在参考存档上测过 60 个有固定词条
     的种类：这些字段在每个副本间完全一致（58/60，两个例外是 八咫镜 的两个变体，按「不一致」拒绝）；
     而 metadata 第 10/11 字节（只有 25/60 个种类一致）与槽位 `prefix`（4 个种类不一致）
     是**逐件字段**，因此一个字节都不动。目标种类在存档里没有实例时直接拒绝——绝不猜固定词条。
  3. **普通词条与末位恩宠/套装槽保持原样**：只写头部 id（两份镜像）与固定槽。
     副本验收：记录 #3 `龙笛[武士] → 八尺琼勾玉[武士]` 只变 **7 个字节**
     （头部 4 字节 + 固定槽的 id/数值 3 字节）+ 校验和 3 字节，读回是
     `0x4987 八尺琼勾玉[武士]`、固定词条 `id=0x53d4 数值=1`，槽 1（普通词条）与
     槽 4（不动明王的恩宠）原样保留。
* **`0x5c5f` 与 `0x1521` 查清了**：`0x5c5f` 带 `格挡可增加灵力 +10` → 是 八咫镜[**武士**]；
  `0x1521` 带 `识破可增加灵力 +10` → 是 八咫镜[**忍者**]。所以之前「1 件对不上」是**原始表没收录
  0x5c5f**，而不是数据错；工具仍按「不在物品种类表内」如实显示（并因此拒绝把它当互换的起点/终点）。

* **显示「这是什么饰品」（种类，只读）**：v2.21 记录头 `+0x00`（`+0x02` 是它的镜像，
  213 件全部相等）本来就是**物品 id**，不是参考项目捕获的类别码。新增
  `data/accessory_items.json`（`物品总目录` 的饰品行：89 行 → 88 个 id，
  武士饰品 46 / 忍者饰品 42）与 `ItemDb`，`list` 与 GUI 现在会把每件饰品写成
  `种类 0x3e3f 龙笛[武士]`、`0x4987 八尺琼勾玉[武士]`、`0xf5bb 凶王耳饰[忍者]`。
  在参考存档上实测：213 件里 **213 件**都能对上（此后 `0x5c5f` 已按存档证据补入，
  见下方「八咫镜两个 id 按实测拆分」），原始表里没有该 id 时报告会如实写
  「不在物品种类表内」，不猜。
  这条同时交叉验证了之前的游戏内观察——记录 #3 就是那支「龙笛」。
* **种类只读**：工具**不会**改饰品种类/等级/品质（`+0x00/+0x02/+0x04/+0x06/+0x08/0x30/0x31`
  一律不写）。原因写在 README 的边界说明里：镜像字段游戏读哪个、换完种类后原有词条
  是否必须配套、专属套装词条是否要跟着换、等级/品质是否要一致改写，以及 `0x1521`
  一个 id 同时对应 八咫镜[武士] 与 [忍者]（光靠 id 定不了物品），都还没验证过。
  要做得先在存档副本上写入并在游戏内确认。
* `tools/inspect_save.py` 新增「饰品是什么（种类，只读）」统计，并顺手修正了
  「N 种」只数了截断列表的问题（恩宠段与种类段现在都报真实种类数：
  参考存档是 231 个末位槽 / 43 种 id、213 件饰品 / 74 种种类）。
* 测试：`tests/test_affixdb.py::ItemCatalogTests`（11 项）、
  `tests/test_build_affix_db.py::ItemCollectionTests`（5 项，含「已提交的
  accessory_items.json 必须与原始表一致」的漂移检查与 `0x1521` 冲突可见性）、
  `tests/test_editor.py::ItemKindDisplayTests`（5 项）、
  `tests/test_ui.py::ItemKindWidgetTests`（4 项）、CLI/inspect 的种类断言，
  以及「物品 id 既不进合法词条库、也不进恩宠表」的越权防护断言。
* **可以合法地改恩宠了（恩宠 → 恩宠）**：饰品的末位槽如果是 `xxx的恩宠`，现在可以
  换成另外 21 个恩宠之一（`词条总目录` 里 `[恩宠]` 10 条 + `[上位恩宠]` 11 条）。
  专属套装词条（如 `怨恨盖世`，用户说的「凶王盖世」那类，`[忍者套装]`）以及普通词条
  **一律拒绝**，并给出理由。判定用三份互相独立的证据，全部通过才允许写：
  1. 槽位必须是该饰品的**末位**且不在 `饰品词条` 表内（否则说明它不是恩宠槽）；
  2. 槽位的**族字节**（metadata 第 9 字节）必须是恩宠族 `0x0C` —— 真实存档里 196 个
     恩宠槽全是 `0x0C`，16 个套装槽全是 `0x4C`，所以套装槽天然被挡住；
  3. 槽位当前 id 与新 id 都必须落在 `grace_affixes.json` 的恩宠/上位恩宠里。
  写入只改 `effect_id` 与 `value`（恩宠数值恒为 0），族字节/子类字节与尚未搞清楚的
  第 11 字节**原样保留**（同一个恩宠在不同饰品上第 11 字节各不相同，说明它不属于恩宠
  身份，不能凭空编造）。在真实存档的**副本**上跑完整写入流程验收：9.4 MB 的存档
  明文只变了 **5 个字节**（槽位 id 2 字节 + 校验和 3 字节），metadata 保持
  `0x5c020c00` 不变。
* CLI：`edit --record N --grace <id|名称>`（`--grace 0x4fa3` / `--grace 稻荷神`，
  名称不唯一会列出所有候选）；`--edit` 与 `--grace` 二选一，同时给或都不给都会报错。
* GUI：右侧新增「恩宠（末位槽）」栏 —— 下拉列出 21 个恩宠并预选当前恩宠，
  可改时按钮可用、状态行显示「当前末位槽 [4]：不动明王的恩宠（0x71f6）」；
  不可改时按钮置灰并显示原因（如「末位槽 [4] 是套装/专属套装词条（怨恨盖世，
  id=0xa7a1），按规则不能改成恩宠」）。应用后只改内存数据，仍由「写入存档」落盘。
* **恩宠/套装词条现在显示具体名字**：原始表 `词条总目录` 里本来就有
  `[恩宠] 10 条 / [上位恩宠] 11 条 / [武士套装] 34 条 / [忍者套装] 22 条`，只是不在
  `饰品词条` 页里，所以之前只能显示成「未知/表外」。新增
  `data/grace_affixes.json`（77 条，由 `tools/build_affix_db.py` 一并生成）与
  `GraceDb`，GUI/CLI 现在显示如
  `0x71f6 不动明王的恩宠（上位恩宠）`、`0x4fa3 稻荷神的恩宠（恩宠）`。
  真实存档的 231 个末位槽里 215 个能对上名字，剩 12 种 id（16 件）原始表里也没有，
  仍按 `恩宠/套装词条（表外，id=…）` 老实显示，并在 inspect 报告里单列出来。
  其中 `[恩宠]/[上位恩宠]` 21 条同时是上面那条「改恩宠」功能的合法目标，
  `[武士套装]/[忍者套装]` 56 条仍然**只用于显示**、绝不作为写入依据。
* `tests/test_editor.py::GraceEditTests`（16 项）、`tests/test_ui.py::GraceWidgetTests`
  （7 项）、`tests/test_cli.py` 的恩宠端到端用例：覆盖「恩宠可改」「恩宠↔上位恩宠互改」
  「专属套装被拒」「普通词条被拒」「族字节不是 0x0C 被拒」「名表外的 id 被拒」
  「非饰品记录被拒」「写入只动 id 字节、metadata/value 不变」「名称解析与歧义提示」。
* `tools/inspect_save.py` 的末位槽统计带上了名字，并汇总「名表未收录」的 id。
* `tests/test_build_affix_db.py`：新增「已提交的 JSON 必须与原始表一致」的漂移检查
  （两个表都查），以及恩宠/套装解析、"恩宠 id 不得进入合法词条库" 等断言。

* **简体中文版 README（`README.zh-CN.md`）**：与英文版 `README.md` 逐节对应
  （878 行 / 60 个标题 / 13 个代码块 / 51 行表格，层级与顺序一致；全部数字、
  命令、十六进制偏移、JSON 键与 Python 标识符保持原样），两份文件首行互相切换。
  同时把 `README.zh-CN.md` 加进随 exe 解压的载荷与构建冒烟检查清单，
  发行包里现在同时带英文与中文文档。
* **面向发布的措辞整理**：取证所用的那一份存档在 README、CHANGELOG、代码注释与
  运行时提示里统一称为「参考存档 / the reference save」（不再出现把某份存档写成
  某个报告者文件的说法），README 新增「参考存档」定义小节；对话客体是使用本工具的
  用户，而不是提供样本的人。提交信息同样按此措辞撰写。

### 修复

* `save_catalog()` 之前用平台默认换行写 JSON，Windows 上会把已提交的 LF 文件改成
  CRLF；现在固定 `newline="\n"`，重新生成不会产生无意义的整文件差异。
* `build.ps1` 的单文件冒烟测试现在也检查 exe 是否解压出 `data/grace_affixes.json`。
* **GUI 在非全屏窗口下「操作按钮不见了」**：状态栏原本与「仅演练 / 写入校验 /
  应用修改 / 写入存档」挤在同一行并用 `pack(side=LEFT)` 排布，于是读取饰品后
  变长的状态文字（「已读取 213 件饰品 · 另有 1243 条…」）把整排控件挤出了窗口
  右边界。现在：底部各条改为**先按 `side=BOTTOM` 分配**（窗口再矮也不会被压掉），
  状态文字独立成行并自动换行、记录表详情另起一行，操作行永远只有四个控件。
  实测 980×520 的窗口下操作行仍完整可见（右边界 402 / 980）。
* **每件饰品末尾的「恩宠/套装组合」词条不再显示为「未知词条」**：只有整条记录都不
  命中词条库时才按「未知词条 / 非表内词条」显示（那种记录本来就不是饰品，不能靠猜）。
  该槽在 GUI 里额外提示「改选表内词条会把它替换掉」。
* **同上，`is_accessory is None` 不再被当成「不是饰品」**：没提供词条库时按旧行为
  保留记录（这是上次测试里弹出模态对话框的根因）。
* **GUI 测试不会再在屏幕上留下任何窗口**：所有 `messagebox` 入口统一 stub
  （`tests/support.silence_dialogs`），窗口统一 `withdraw()`；此前一个未预期的对话框
  会弹出真实模态窗口、卡住跑测试的机器直到有人点掉它。

### 新增（GUI 状态）

* **GUI 实时显示游戏是否在运行**：底部新增状态行（绿=「未检测到仁王3 进程——可以
  写入存档」，红=「<进程名> 正在运行——写入会被拒绝…」），每 3 秒在后台线程复查一次
  （`tasklist` 不进 UI 线程），并且不会覆盖用户操作的状态文字。

* **真实 v2.21 存档：步长是 `0xF0` 而不是捕获的 `0xE8`，一条饰品都读不到**。
  拿一份真实 v2.21 存档（`0x9001B0` 字节、校验和一致）逐字节核对后确认：该版本的
  记录表在 `0x270066`，**记录步长 `0xF0`**（捕获版本为 `0x176CCE` / `0xE8`）。
  `0xE8 × 30 == 0xF0 × 29 == 6960`，所以按 `0xE8` 扫描时每 6960 字节才撞上一条真
  记录——旧代码在真实存档里看到 49 个候选、识别出 13 条错位的「记录」，于是表现为
  「点读取饰品，未读取到」。现在 `locate_layout` 会**先探测步长**（`0xE0`/`0xE8`/
  `0xF0`/`0xF8`/`0x100` 各扫一遍，取「最大余数类记录数」压倒性胜出的那个），再在该
  步长下按余数类定位记录表；真实存档上 `0xF0` 得 1457 条、`0xE8` 只有 58 条。
* **真实 v2.21 存档：`record+0x00` 不再是捕获里的分类 id，改用词条库证据判定饰品**。
  真实存档里该字段是**每件物品自己的 id**（如 `0x6FA5`，并且自身重复存成
  `a5 6f a5 6f`），不再是 `{0x0000,0x1E82,0x516D,0xE604,0xDD82,0xD523}` 这些捕获到的
  绘卷/装备分类 id。由于随包词条库只有**饰品词条**，现在把「某个已占用效果槽的
  `effect_id` 命中词条库」作为饰品证据：真实存档 1456 条物品记录里 **213 条**判定为
  饰品（其余 1243 条武器/防具/绘卷**零命中**，区分干净），`list`/GUI 只列出这 213 条并
  报告另有多少条属于其它物品。没有提供词条库时仍保留旧的分类标签（`--python-crypto`
  等旧路径不受影响）。
* **同一份存档里的多张表不再被「最密的那张」抢走**：对齐类改为按「含饰品词条的记录
  数」优先排序（其次才是记录数、捕获对齐），所以当某张表记录更多但不含饰品时，仍会
  选中真正含饰品的那张。
* **`scan` 在 `0xF0` 存档上会抛 `RecordError`**：`layout_diagnosis` 之前按
  `layout.stride` 切片（0xF0 > 记录实际内容 0xE8），已改为固定按记录内容长度 `0xE8`
  切片——步长只决定下一条记录的位置，不决定本条记录有多长。证据说明里写死的
  「同一 0xE8 对齐」也改为按实测步长输出。

### 新增

* **`tools/inspect_save.py`（只读深度检查）**：不改写、不联网，一次回答三个问题——
  记录表在哪、效果槽基准是哪一个、哪些记录算饰品。含**效果槽基准搜索**（对每个候选
  基准统计「该位置的 u32 是词条库内 id」的命中数与命中槽位，并按捕获的 `0x34` 优先
  排序、标出与其同格的位移别名）、按记录类型的汇总（数量/等级/品质/占用槽位直方图/
  命中词条库比例）、逐条记录与全部 7 个效果槽的 `id/数值/标识`，以及**库外 id 统计**
  （存档里出现但随包词条库没有的 id 及出现次数，用来反馈词条库缺口）。
* **记录表改为「定位」而非「写死偏移」**：此前记录区起点是参考项目在
  v2.00.02/2.01 存档里捕获的常量 `0x176CCE`，而该偏移是**版本相关**的——更新的游戏
  版本一旦移动记录表，工具就会把整段读成空白（表现为「点读取饰品，一条也没读到」）。
  现在改为在**所打开的存档里**按记录头特征搜索记录表：
  * 判定沿用参考项目识别真实记录的条件：`type@0 == type@2`、`level@6 == level@8`、
    type 非 0，且为绘卷类型或 `@4 == 1`。最后一条很关键：空闲效果槽里
    是 `0xFFFFFFFF`，自身镜像，不加这条会把槽内字节误认成嵌套记录；而 `@4` 正是账号
    id 的中间 16 位（此量级的 Steam64 id 恒为 `0x0001`），所以它等价于「属于本账号」。
  * 命中项按「偏移 mod 步长」聚类（同一张记录表共用一个余数），再按该类内的有效记录
    数确认对齐；随机字节命中概率约 `1/2^48`。
  * 定位失败**宁可失败也不乱写**：抛错并提示运行 `scan`，绝不按猜到的字节修改存档。
  * 记录表若仍在参考项目捕获的位置**且步长也是捕获的 `0xE8`**，槽号沿用该表自身编号
    （卖掉最前面的物品也不会让其它记录的槽号漂移）；否则以第一条已占用槽为锚点，并在
    报告里说明步长与不对齐的原因。
  * 读取/写入改用**记录自身的绝对偏移**（`EditPlan.offset`），不再由槽号反推。
* **`scan` 诊断命令（只读）**：`python launch_editor.py scan [--json] [--preview N]`
  打印存档大小、记录头命中数与前几处偏移、定位到的记录表（锚点/槽数/步长/是否与捕获
  布局对齐）、**证据强度**（命中太少会明说「证据较弱」）、记录类型与品质分布、等级
  范围、`+0x0E` 符号位（参考项目的「未完成」标记）计数，以及若干条记录的 7 个效果槽
  原始字节。**读取存档文件即可，游戏无需运行**；结果不符预期时可直接把输出反馈。
* **GUI 不再「静默空表」**：读不到记录时分两种情形给出可反馈的说明——找到记录表但没有
  可编辑记录（弹窗 + 状态栏说明记录表信息），以及完全定位不到记录表（弹窗附完整诊断，
  并提示运行 `scan`）。记录列表的「类型」列同时标出记录族（绘卷 / 装备·饰品）与 type。
* **原始数据入库**：`third_party/source-data/` 收纳《仁王3词条装备库v2.21.xlsx》与
  `Nioh3 v2.21.CT`（含来源与授权说明），词条库可离线重新生成，工程不再依赖外部目录。
* **写入前置条件提示（退出游戏 / 标题界面）**：存档编辑最终落在文件上，因此游戏不能
  正在内存里持有该存档。现在写入前会明确提示「请先完全退出游戏，或退回到游戏标题
  界面」并说明游戏内保存会覆盖本次修改：
  * CLI `edit`：写入前打印完整要求 + 当前门禁状态（是否检测到游戏进程、是否演练模式），
    成功后再提示「加载前请勿在游戏内保存」；
  * GUI：新增常驻红色提示行（始终可见，不只在弹窗里），写入确认对话框内嵌完整要求；
    检测到游戏运行时额外说明"只有标题界面写入才安全"；
  * 文案集中在 `savefile.SAVE_WRITE_REQUIREMENT`，CLI/GUI/拒绝信息三处保持同一口径。
* **单文件 exe（发行形态）**：发行包只包含 `Nioh3AccessoryEditor.exe` 一个文件，不再有
  任何 `.py`、目录或安装步骤。
  * 首次运行把随附文件解压到 **exe 同目录**：`config/editor.json`（参数配置）、
    `data/accessory_affixes.json`（词条库）、`bin/Nioh_Savefile_decrypt.exe`
    （随附加解密组件）、`third_party/source-data/`（原始 xlsx/CT）、`README.md`、
    `CHANGELOG.md`，并写入 `.extracted-manifest.json` 记录清单。
  * 解压**不会覆盖用户改过的文件**：清单里记的是本程序写入的哈希，内容一致才刷新；
    删掉的文件会重新解压出来；目录只读时降级为「只从 exe 内部读取」而不报错。
  * 控制台子系统：从终端运行时保留控制台（CLI 可用），双击启动 GUI 时自行隐藏控制台
    （仅在独占该控制台时隐藏，不影响用户的终端）。
* **参数配置文件 `config/editor.json`**：存档目录、账号/栏位过滤、加密后端、备份目录、
  GUI 演练/校验默认值都可在文件里改；命令行参数始终优先。
  * 新增 `config` 子命令：`config`（查看有效配置）、`config --json`、
    `config --init [路径] [--force]`（生成带说明的默认文件）。
  * 新增全局参数 `--config <路径>`，可指定其它配置文件。
  * 文件校验严格：`schema` 不匹配、未知键（如把 `default_dry_run` 写成 `dry_run`）、
    类型错误都会在启动时报错，不会静默忽略；`_` 开头的键视为注释。
    相对路径相对 exe 目录解析，便于整个文件夹搬迁。
* **`build.ps1` 新增 `-TestPattern`**：只跑匹配的测试模块（如 `-TestPattern test_cli.py`），
  便于单模块迭代。
* **程序图标与 logo（勾玉）**：新增 `assets/`，全部由 `tools/make_icon.py` 用标准库几何绘制
  生成（可复现、可复核，**不是游戏素材**）：
  * `app.ico`：16/20/24/32/40/48/64/128/256 共 9 种尺寸，编译进 exe，资源管理器、
    任务栏、标题栏都显示；
  * `logo.png` / `logo-64.png` / `logo-32.png`：GUI 标题区与窗口图标使用；
  * 小尺寸单独调整（加粗玉体、去掉穿绳孔与描边），保证 16 px 下仍能辨认；
    `tests/test_icon.py` 用金色像素占比守住这一点。
* **GUI 品牌区**：窗口/任务栏图标取自 `assets/app.ico`（读取失败时回退 `logo-32.png`），
  顶部新增 64 px logo + 标题 + 副标题；资源缺失时自动降级为文字，不影响使用。
* **`tools/check_exe_icon.py`**：直接解析 PE 资源目录（`RT_GROUP_ICON` / `RT_ICON`），
  校验 exe 真的带图标、9 种尺寸齐全、且与 `assets/app.ico` **逐字节一致**——不再依赖
  “PyInstaller 应该会嵌入”的假设。
* **备份可恢复（`restore`）**：此前只有「写入前自动备份」，没有恢复入口——备份是**解密明文**，
  不能直接覆盖回加密的 `SAVEDATA.BIN`。现在补齐了恢复这一半：
  * `savefile.list_backups()`：按账号/栏位枚举历史备份（新→旧），逐个核对清单里的
    SHA-256，损坏的会标注并拒绝用于恢复；
  * `editor.restore_backup()`：校验备份 → 同一套门禁（游戏未运行、静止性、`SAVE_SYNC_ACTIVE`）
    → **先给当前存档再做一次备份** → 重新加密写回（原子替换 + 写入前后校验 + 失败回滚）；
  * CLI `restore`：`--list` 列出、`--from <序号|latest|路径>` 还原、`--dry-run` 演练；
  * GUI 新增「恢复备份」按钮：列出备份（时间 + 大小 + 校验状态）→ 二次确认 → 还原；
  * 报告里 `matches_original_save` 会说明还原后的文件是否与**备份时记录的原文件**逐字节一致。
* **乱码防护测试**：新增 `tests/test_text_encoding.py`，扫描全部文本文件，检测
  「私用区字符」「汉字紧邻 ASCII `?`（丢字节）」「GBK 往返可解的乱码串」三种特征。
  规则是量出来的：对修复前的 README 报出 32 处，对现在的仓库报 0 处，并且用真实
  乱码样本 + 正常中文样本各测一遍，保证「能报」且「不误报」。

### 变更

* **版本信息里的路径改为「运行时」路径**：`来源` 现在显示**正在运行的这个程序所处的
  目录**（冻结运行时就是 exe 所在目录），`加密组件` 显示该目录下实际读取的加解密组件
  路径——把 exe 复制到别处，显示的路径随之前移，无需重新构建。构建时的工程目录仍然
  保留，但只在两者不一致时作为额外一行 `构建来源` 出现（`BUILD-INFO.txt` 里始终记录
  构建时路径）。四要素（commit 后 8 位 / 来源 / 构建时间 / 语言）不变。
* **`build.ps1` 默认不再跑单元测试**：整套测试要 4 分钟，而打包各步骤并不依赖它，
  每次打 exe 都等一遍没有必要。现在测试是**可选**的：加 `-Test` 才跑（`-TestPattern`
  与 `-PureCryptoTests` 隐含 `-Test`）；`-SkipTests` 保留但已成空操作，旧命令不会报错。
  为避免「没跑测试的构建被误当成已验证」，构建结束的版本报告会明确打印
  `单元测试  : 已运行 / 未运行（默认跳过；发版前请加 -Test）`，发版流程用
  `build.ps1 -Clean -Test` 即可。
* `tools/build_affix_db.py`：默认来源改为仓库内的 `third_party/source-data/`
  副本（保留旧路径作为回退），新增 `resolve_source()` 与找不到文件时的路径提示。
* 游戏进程门禁统一为一处实现：`editor.commit_save` 改为调用
  `savefile.require_game_not_running()`，不再维护第二份判断与文案。
* `build.ps1`：新增「校验 assets/ 与生成器一致」与「校验 exe 内嵌图标（PE 资源）」两步，
  冒烟测试额外断言 `assets\app.ico`、`assets\logo-32.png` 已解压到 exe 同目录。
* `build.ps1`：发行流程改为「生成构建信息 → 跑测试 → 生成内嵌载荷
  （`tools/make_payload.py`）→ PyInstaller 单文件打包 → 在全新目录冒烟测试 exe
  （校验自解压产物与冻结的构建信息）→ 校验发行物无 `.py` → 打包 zip」，不再复制源码目录。
  * PyInstaller 只作为构建期依赖，自动装入 `.build-venv`（可用
    `-PyInstallerPython` 指定解释器；镜像源可用 `NIOH3_PIP_INDEX` 覆盖）。
  * **修复了一处会让失败构建看起来成功的缺陷**：Windows PowerShell 5.1 下
    `& exe 2>&1 | ForEach-Object` 这种合并输出写法可能返回过期的 `$LASTEXITCODE`（子进程
    实际退出码非 0，PowerShell 却报告 0），导致单元测试失败时构建仍继续出包。现在改为
    `Start-Process -Wait -PassThru` 读取真实退出码，并在构建开始时用
    `tools/check_build_gate.py` **自检**「失败的子步骤一定会被中止」，自检不过直接中止构建。
* `nioh3_accessory_editor/paths.py`（新增）：统一「源码布局」与「冻结布局」的路径解析，
  资源一律以 exe 同目录为准。
* `nioh3_accessory_editor/bootstrap.py`（新增）：随附文件的自解压与清单管理。
* `nioh3_accessory_editor/config.py`（新增）：参数配置的读取、校验与默认值生成。
* `savefile.default_crypto_tool()` / `savefile.discover_save_paths()` /
  `editor.discover_saves()`：改为按冻结布局与配置解析路径（`discover_save_paths(root=...)`、
  `discover_saves(root=...)` 可按配置指定存档根目录）。
* `version.py`：冻结运行时不再尝试调用 `git`，构建身份只取自 `_buildinfo`；版本信息额外
  显示程序目录与配置文件路径。
* GUI 的备份目录、演练/校验默认值改由配置决定（源码运行时仍为当前目录）。
* `savefile.write_encrypted_save()` 的尾字节参数由布尔改为显式 `tail=`：
  实测参考解密工具在**加密与解密两个方向都会把存档最后 8 字节清零**（该 8 字节不参与
  加密），因此「沿用当前文件尾字节」原本是硬编码行为，现在改为可显式传入。
  普通编辑仍沿用当前文件的值；恢复则写回备份里记录的尾字节（备份里存在时）。
* CHANGELOG 合并了两段重复的「变更」小节，顺序回到「新增 / 变更 / 修复 / 移除」。

### 修复

* **「读取饰品」一条记录也读不到**：记录区起点原本是参考项目在 v2.00.02/2.01 存档里
  捕获的常量 `0x176CCE`，而参考文档明确写着该偏移**版本相关、更新后必须重新采集**。
  更新的游戏版本一旦移动记录表，工具就会在错误的偏移上读取并得出「0 条记录」（而且
  旧代码在存档小于该偏移时会直接报错、GUI 只显示一张空表）。现在记录表是**在存档里
  搜索出来的**，详情见上面「新增」小节；定位不到时**失败关闭**并给出 `scan` 提示，
  GUI 也会弹出可反馈的诊断，不再静默显示空表。
* **README 大面积 GBK 乱码**：早期用 PowerShell 写文件时把 UTF-8 当成 GBK 解码后再保存，
  导致 README 里 40 余处中文变成乱码（部分字节丢失，显示成 `?`）。已按「GBK→UTF-8
  回解 + 与 git 历史/源码文案逐条核对」的方式修复，并用脚本全仓库检查
  （私用区字符、紧邻 `?` 的汉字）确认无残留。
* **GUI 启动时保存扫描必然失败**：`ui._load_saves` 是 `@staticmethod` 却引用了 `self`，
  于是每次启动都抛 `name 'self' is not defined` 并弹出「错误」对话框（存档列表永远为空）。
  该缺陷此前被所有 GUI 测试掩盖——它们都 stub 掉了 `refresh_saves`。现改为实例方法，并补上
  两处防护：真正走启动扫描的回归测试，以及「package 内不允许 staticmethod 引用 self」的
  静态检查测试。
* `ui._load_image` 未指定 `master`：`tk.PhotoImage` 会绑定到默认解释器，在存在多个 Tk
  解释器时图片不可用；现在显式绑定到本窗口。
* GUI 未发现存档时，状态栏改为显示**实际查找位置**（`未发现存档 · 查找位置 …`），
  而不是只报一句「未发现存档」。

### 移除

* 删除同工作区的 `Nioh3Trainer\` 工程（C++ 内存修改器框架，偏移全为占位符、从未生效，
  且不是 git 仓库）：其功能已被本工程取代。删除前已把其中两个不可再生的原始数据文件
  迁入 `third_party/source-data/`，并核对迁移后词条库重新生成结果与已提交内容逐字节一致。

## [0.1.0] - 2026-09-19

首个版本：仁王 3 PC 存档饰品词条修改器，编辑结果直接写入存档文件（非仅内存），
纯标准库 Python + Tkinter。

### 新增

* **存档读写管线**（`nioh3_accessory_editor/`）
  * `nioh_aes.py`：定制 AES 的表驱动实现（自实现 S 盒/RS 盒、字序反转的初始轮密钥、
    列旋转式 ShiftRows）。
  * `crypto.py`：参考实现的计数器异或流，双密钥对（header 用固定根密钥，body 取自
    明文头 `0x49/0x59/0x69/0x79`）；覆盖区间 `[0, 0x158 + 0x900050)`，尾部 8 字节
    在所有变换中原样保留。
  * `checksum.py`：body `[0x190, 0x900190)` 按 `0x400` 分块的 `<q` 求和与 seed 折叠，
    并提供只读校验 `verify_user_checksum`。
  * `records.py`：记录区 `0x176CCE`、400 槽 × `0xE8`、每记录 7 个 `0x18` 词条槽；
    编辑字典严格校验（未知字段、非整数、越界、重复槽位一律拒绝）。
  * `savefile.py`：存档发现、双加解密后端（内置参考 exe / 纯 Python）、静默期双读
    指纹、游戏进程门禁、明文备份 + manifest、写入前后解密校验与失败回滚、
    `MoveFileExW` 原子落盘。
* **编辑器三层**（`editor.py` / `cli.py` / `ui.py`）
  * CLI 子命令 `list` / `check` / `edit` / `backup`，全部失败关闭（fail closed）。
  * Tkinter GUI：常驻免责声明、默认「仅演练」、词条下拉选择、应用修改后再写入存档。
* **词条库**：`tools/build_affix_db.py` 从《仁王3词条装备库v2.21.xlsx》饰品词条表
  生成 `data/accessory_affixes.json`（276 条合法词条，schema
  `nioh3-accessory-affixes/v1`）；表外词条一律拒绝。
* **测试**：`tests/` 282 个用例，`python tools/run_tests.py`（可选
  `--pure-crypto` 跑慢速全文件纯 Python 路径）。
* **重量级交叉校验**：`tools/crosscheck_crypto_vs_exe.py` 对 9,437,608 字节的
  加密覆盖区做纯 Python 与参考 exe 的逐字节比对。
* **构建与版本信息**：`build.ps1` + `tools/make_build_info.py`，版本信息输出
  commit 后 8 位、来源（项目根 + 加密 exe 路径及其 SHA-256）、构建时间、编程语言；
  同时显示在 CLI（`--version` / `version` / `--json`）、GUI 底栏与「版本信息」按钮、
  `BUILD-INFO.txt|json` 与构建日志中。

### 修复

* **header 末 8 字节未被加密**：异或长度误用整除（`length // 16`）而非向上取整，
  导致 `[0x150,0x158)` 保持明文；由全量 exe 交叉校验捕获（`0x150` 起 8 字节不一致），
  改为向上取整并按精确长度异或。
* **GUI 崩溃**：`_populate_accessories` 先把 `selected_accessory` 置空、随后又用它设置
  树选中项，触发 `TclError: Item None not found`；现保存目标索引并在重建树后显式恢复。
* **GUI 每次应用都重写全部 7 个槽位**：改为与当前记录比对，只提交真正改动的槽位；
  重选同一条词条、清空已空的槽位都不再产生写入。
* **GUI 臆测 metadata**：原先会把词条库的 固定/星 标志位合并进 `metadata`，但该位在
  存档中的编码尚未核实（且 `星 = 0x04` 与类别位重叠）；现 GUI 只写有据可依的
  `effect_id` + 标称 `value`，`metadata` 保持原样，CLI 可显式指定。
* **`--edit` 指向不存在的记录会被静默忽略**：现在显式报错；同时修正
  `plan_edits` 会伪造 `item_count=1` 的行为（新增 `records.read_item_record`）。
* **校验和语义误导**：`commit_save` 的 `checksum_was_consistent` 实际是对「入参缓冲」
  的度量（编辑后必然为 False），已改名并明确语义。
* **游戏进程检测重复**：大小写变体导致同一进程名出现两次，已去重。
* **词条库重复 ID**：生成器改为按 `effect_id` 去重（严格模式），同 ID 不同
  数值/标志的行记入 `conflicts` 而不是静默取其一。
* **CLI 退出码/路由**：`version` 子命令未登记到 CLI 路由集合导致误入 GUI；已修复并
  新增「CLI 解析器的所有子命令都必须可路由」的测试。
* **`--version` 输出被重排**：argparse 内置 version action 会把多行横幅交给 help
  formatter 折成一段，改为自定义 action 原样打印。

### 安全 / 可靠性

* 写入前检查存档静默期（存档 + `BACKUP.BIN` + 账号系统存档双读指纹），检测到游戏
  正在保存则中止（`SAVE_SYNC_ACTIVE`）；指纹计算会重新 `stat` 以发现竞态。
* 检测到 `Nioh3.exe` / `Nioh3-Win64-Shipping.exe` 运行时拒绝写入，除非显式
  `--force-while-running`。
* 每次写入前生成明文备份 + manifest（含主存档与明文的 SHA-256），备份文件名带
  时间戳与随机后缀，避免并发覆盖。
* 写入流程：暂存 → 解密校验 → 重新校验指纹 → 原子替换 → 再次解密校验 → 失败回滚。
* 记录解析失败即失败关闭（header 不匹配不猜测）；GUI 常驻免责声明并默认演练模式。

### 说明（已知边界）

* **真实存档中的饰品词条槽布局尚未用实机存档核对**：偏移来自参考工程的卷轴布局与
  作弊表的装备结构，且记录区包含全部装备族，`list` 会列出所有匹配捕获头的记录。
  请先在自己的存档上运行 `list` / `check` 核对，并保留自动备份。
* 参考实现的「解密」不是其「加密」的逆：其生效的 `rsbox` 与 `sbox` 完全相同，且该
  自定义 S 盒不是置换（164 个不同值、69 个重复值、92 个值不可达），信息在 SubBytes
  阶段即不可恢复。因此本工程按参考的实际行为，用同一 ECB keystream 异或实现解密；
  `NiohAes.decrypt_block` 仅用于忠实复刻 `aes.c`，管线中从不调用。

---

## 版本号与发布流程

1. 修改 `nioh3_accessory_editor/version.py` 里的 `__version__`。
2. 在本文件顶部新增对应版本小节（`## [x.y.z] - YYYY-MM-DD`），把「未发布」内容
   移入该小节。
3. 运行 `powershell -File .\build.ps1`：跑测试 → 生成构建身份 → 组装并冒烟测试
   `dist/` → 打包 zip。缺少当前版本小节时 `tests/test_changelog.py` 会失败。
4. 提交（本仓库只做本地提交，不打 tag/不发远端）。
